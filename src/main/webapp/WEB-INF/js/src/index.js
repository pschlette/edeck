// @flow
import React from 'react';
import ReactDOM from 'react-dom';
import { placeholder } from 'flowTypes';
import App from 'components/App';
import './index.scss';

ReactDOM.render(
  React.createElement(App),
  document.getElementById('root')
);
